type Book {
    id: String!, # If you want to specify a non-nullable field, you need to use "!".
    title: String!,
    author: String!
}
# The Root Query for the application
type Query {
}

type Mutation {
    deleteBook(id: String!): Book!
}
