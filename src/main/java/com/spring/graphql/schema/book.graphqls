type Book {
    id: ID!, # If you want to specify a non-nullable field, you need to use "!".
    title: String!,
    author: String
}
# Query or Mutation objects are root GraphQL objects.
type Query {
    book:Book
    books(id: ID):Book
}

type Mutation {
    addBook(title:String, author:String):Book
}
